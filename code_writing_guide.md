#📖 C++ Development Guide: Стандарт Команды
Привет, коллега! Как твой Тимлид, я подготовил этот гайд. Мы пишем код не для компилятора, а для людей. Следуй этим правилам, и твой код будет радовать глаз на любом Code Review.
1. Именование (Naming Conventions)
Мы используем разные стили, чтобы сразу понимать природу сущности без чтения её объявления.
Общие категории
Классы и Структуры (PascalCase): Имена — существительные.
Пример: UserManager, DatabaseConnection, PhysicsEngine.
Функции и Методы (camelCase): Имена — глаголы.
Пример: calculateTotal(), sendRequest(), updatePosition().
Локальные переменные (snake_case): Коротко и ясно.
Пример: retry_count, current_user_id, buffer_size.
Константы и Макросы (SCREAMING_SNAKE_CASE):
Пример: MAX_BUFFER_SIZE, DEFAULT_TIMEOUT_MS.
Приватные поля классов: Префикс m_ (member) + snake_case.
Пример: m_is_connected, m_retry_limit.
Глобальные переменные: Префикс g_ + snake_case.
Пример: g_config_path, g_main_logger.
Не создавать забранированные переменные: Префикс _  или __ + Заглавные буква они зарезераированы реализацией. 
Логические (bool) операции и переменные
Для булевых типов мы используем префиксы-вопросы. Это делает условия читаемыми как человеческая речь.
Префиксы: is, has, can, should.
Примеры:
bool is_active;       // ХОРОШО (локальная переменная)
bool has_permission;  // ХОРОШО
bool g_is_running;    // ХОРОШО (глобальная переменная)

if (user.has_permission()) { ... } // Читается как "Если пользователь имеет разрешение"


2. Глобальные переменные и extern
Глобальные переменные — это зло, которого стоит избегать. Если они всё же необходимы:
Использование extern: Если тебе нужно использовать переменную в нескольких файлах, объявляй её в .h файле через extern, а определяй в одном .cpp файле.
Инкапсуляция: Вместо того чтобы выставлять vector<int> g_numbers наружу, лучше создай функцию или класс-синглтон, который дает доступ к этим данным. Это защитит данные от случайной перезаписи.
Статическая глобальность: Если переменная нужна только внутри одного файла, используй static или анонимный namespace, чтобы она не "торчала" наружу.
``` ```
3. Принцип «Бойскаута»
"Оставь место стоянки чище, чем оно было до твоего прихода."
Если ты зашел в старый файл, чтобы поправить баг или добавить фичу, и увидел там беспорядок (плохой нейминг, лишние отступы, «магические числа») — исправь это. Не нужно переписывать всё, но сделай этот участок кода чуть-чуть лучше.
4. Чистота и структура функций (SRP)
Одна функция делает ровно одно дело.
Пример нарушения: Функция processOrder считает скидку, списывает деньги и отправляет письмо.
Как правильно:
void processOrder(Order& order) {
    applyDiscount(order);
    chargePayment(order);
    sendConfirmationEmail(order);
}


5. Устаревший код (Deprecation)
Если функция устарела, но используется во многих местах, не удаляй её сразу.
Атрибут [[deprecated]]: Помечай старую функцию этим атрибутом. Это заставит компилятор выдавать предупреждение при каждой попытке вызова.
Документирование: Обязательно напиши в комментарии, какую новую функцию следует использовать вместо старой.
Пример:
[[deprecated("Use calculateNewTotal(Order&) instead for better precision")]]
double calculateTotal(int price, int tax) {
    return price + tax;
}


6. Логика и ветвление (Guard Clauses)
Избегай вложенности. Если можно выйти из функции раньше — выходи.
Пример:
// ХОРОШО: Линейный код
void saveData(Data* data) {
    if (!data) return;
    if (!data->isValid()) return;

    // ... работаем ...
}


7. Запрет на «Магические числа» и константы
Числа в коде не объясняют своего смысла.
Где объявлять: Выноси важные значения наверх файла как константы, в специальный заголовочный файл или используй enum class.
Макросы vs constexpr: Мы предпочитаем static constexpr вместо #define, так как они типизированы и безопасны. Макросы используй только если нужно управлять компиляцией.
Пример:
// ХОРОШО: Константа вверху файла или в неймспейсе
static constexpr int MAX_RETRY_ATTEMPTS = 5;

// ХОРОШО: Enum class
enum class UserRole { Admin = 1, Editor = 2, Guest = 3 };


8. Современный C++ (Modern Standards)
Мы пишем эффективно и безопасно.
Умные указатели: Забудь про new и delete. Используй std::make_unique<T>().
nullptr: Никогда не используй NULL или 0.
RAII: Ресурсы должны управляться объектами. Контейнеры (std::vector, std::string) — твои лучшие друзья.
const: Если значение не меняется — оно обязано быть const.
9. Комментарии
ПЛОХО: i++; // увеличиваем i
ХОРОШО: // Используем кэширование, чтобы избежать повторных запросов к БД
Чек-лист перед PR:
Код компилируется без ворнингов.
Имена понятны без словаря.
Все глобальные переменные помечены g_.
Старые функции помечены [[deprecated]].
Соблюден принцип бойскаута.
Вложенность if не глубже 3 уровней.

#pragma once
#include "ui/pro/ProView.h"

// Карта трассы с РЕАЛЬНОЙ линией проезда — аналог Track Report в MoTeC.
//
// Чем отличается от TRACK MAP: та панель отвечает на вопрос «где машина сейчас»
// и потому показывает схему целиком, мелко. Здесь вопрос другой — «как она
// проехала»: полотно трассы рисуется как в Lite (асфальт с бортами), поверх
// него линия, по которой машина реально ехала, и есть режим слежения, в котором
// камера приближается к машине и едет за ней.
namespace Pro {
    void RenderTrackReportWindow(const ProContext& ctx, int32_t vehicleId,
                                 ImVec2 vpSz, float topH);
}
